# MongoDB
EXPOSE_DB_PORT=                   # Leave empty to not expose MongoDB, set to any port number to expose

# Backend
HOSTNAME=localhost     # Host name where backend service is accessible
PORT=3030                         # Port where backend service is accessible
FRONTEND_URL=http://localhost:3000  # URL where frontend is accessible
SMTP_HOST=                        # SMTP server hostname for email sending
SMTP_PORT=465                     # SMTP port (default: 465 for SSL)
SMTP_USER=                        # SMTP username
SMTP_PASS=                        # SMTP password

# AWS Configuration
AWS_ACCESS_KEY_ID=                # AWS access key
AWS_SECRET_ACCESS_KEY=            # AWS secret key
AWS_CLIENT_MODELS_BUCKET=         # AWS S3 bucket name for client models
AWS_REGION=                       # AWS region (e.g., us-east-1)

# Frontend
VITE_APP_API_URL=http://localhost:3030/  # Backend API URL
VITE_MATOMO_URL=http://localhost:7000    # Matomo analytics URL
VITE_MATOMO_SITE=1                                  # Matomo site ID

# FC Worker
BACKEND_URL=http://localhost:3030        # URL where backend is accessible 
